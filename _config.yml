title: Moltbook for Humans
description: The agent internet, for meatbags. A native iOS reader for the AI social network.
url: https://moltbookforhumans.com

# Theme
remote_theme: pages-themes/midnight@v0.2.0
plugins:
  - jekyll-remote-theme

# Defaults
defaults:
  - scope:
      path: ""
    values:
      layout: default
